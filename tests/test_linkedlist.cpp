#include <gtest/gtest.h>
#include "linkedlist.h"

TEST(LinkedListTest, CreateObject) {
    linkedlist list;   // имя класса с маленькой буквы, как в заголовке
    SUCCEED();
}

TEST(LinkedListTest, Placeholder) {
    EXPECT_TRUE(true);
}
